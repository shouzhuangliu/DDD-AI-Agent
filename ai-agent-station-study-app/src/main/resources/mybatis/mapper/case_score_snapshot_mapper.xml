<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.ICaseScoreSnapshotDao">
  <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.CaseScoreSnapshot" useGeneratedKeys="true" keyProperty="id">INSERT INTO case_score_snapshot (case_id,agent_id,total_score,severity_score,negative_feedback_score,frequency_score,importance_score,recency_score,unresolved_age_score,confidence_score,priority_floor_applied,rationale,created_at) VALUES (#{caseId},#{agentId},#{totalScore},#{severityScore},#{negativeFeedbackScore},#{frequencyScore},#{importanceScore},#{recencyScore},#{unresolvedAgeScore},#{confidenceScore},#{priorityFloorApplied},#{rationale},#{createdAt})</insert>
</mapper>
