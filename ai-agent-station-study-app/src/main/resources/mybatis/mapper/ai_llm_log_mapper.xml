<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IAiLlmLogDao">
    <resultMap id="LlmLogMap" type="cn.bugstack.ai.infrastructure.dao.po.AiLlmLog">
        <id column="id" property="id"/><result column="session_id" property="sessionId"/>
        <result column="agent_id" property="agentId"/><result column="model_name" property="modelName"/>
        <result column="mode" property="mode"/><result column="input_tokens" property="inputTokens"/>
        <result column="output_tokens" property="outputTokens"/><result column="total_tokens" property="totalTokens"/>
        <result column="duration_ms" property="durationMs"/><result column="status" property="status"/>
        <result column="error_message" property="errorMessage"/>
        <result column="history_msg_count" property="historyMsgCount"/>
        <result column="folded_msg_count" property="foldedMsgCount"/>
        <result column="system_prompt_len" property="systemPromptLen"/>
        <result column="user_message_len" property="userMessageLen"/>
        <result column="assistant_response_len" property="assistantResponseLen"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>
    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.AiLlmLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_llm_log (session_id, agent_id, model_name, mode, input_tokens, output_tokens, total_tokens, duration_ms, status, error_message, history_msg_count, folded_msg_count, system_prompt_len, user_message_len, assistant_response_len, created_at)
        VALUES (#{sessionId}, #{agentId}, #{modelName}, #{mode}, #{inputTokens}, #{outputTokens}, #{totalTokens}, #{durationMs}, #{status}, #{errorMessage}, #{historyMsgCount}, #{foldedMsgCount}, #{systemPromptLen}, #{userMessageLen}, #{assistantResponseLen}, #{createdAt})
    </insert>
    <select id="queryById" resultMap="LlmLogMap">SELECT * FROM ai_llm_log WHERE id=#{id}</select>
    <select id="queryBySessionId" resultMap="LlmLogMap">SELECT * FROM ai_llm_log WHERE session_id=#{sessionId} ORDER BY id DESC LIMIT #{limit}</select>
    <select id="queryByAgentId" resultMap="LlmLogMap">SELECT * FROM ai_llm_log WHERE agent_id=#{agentId} ORDER BY id DESC LIMIT #{limit}</select>
    <select id="queryRecent" resultMap="LlmLogMap">SELECT * FROM ai_llm_log ORDER BY id DESC LIMIT #{limit}</select>
    <select id="countAll" resultType="long">SELECT COUNT(*) FROM ai_llm_log</select>
    <select id="countByAgentId" resultType="long">SELECT COUNT(*) FROM ai_llm_log WHERE agent_id=#{agentId}</select>
    <select id="countByStatus" resultType="long">SELECT COUNT(*) FROM ai_llm_log WHERE status=#{status}</select>
</mapper>