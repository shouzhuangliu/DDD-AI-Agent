# 记忆折叠 — 推理前历史压缩 + 工具召回（完整·可复现·含代码）

> "记忆折叠"= 每次调 LLM 前，对完整对话历史做的多级递进压缩（核心动词是 fold），配套一条折叠后按 id 捞回原件的闭环（retrieve）。
目的：长对话不撑爆上下文，又不丢当前任务、不破坏消息配对、且被折叠的东西随时可取回。

分支 memory-dev。包 com.ai.agent.harness.history。设计文档 PRJMC0021_HistoryCompressPipeline（初版 2026-05-21，2026-05-31 重构为三层）。

---

## 0. 一句话与全局心法

> **压缩只发生在"发给 LLM 的副本"上——数据库 `chat_message` 里永远是完整原件。** 折叠时刻意保留 `tool_call_id` 作为"取件号"；被折叠/剥空/删除的步骤，大模型可用 `retrieve_tool_call(id)` 按号捞回。

与项目其它两块同一哲学：working context 精简（存摘要+取件号），record 完整（可按 id/路径取回）。三条不可违反的约束：① 当前 user 轮永不删；② `assistant.tool_calls ↔ tool` 配对不能断（每步前后 `sanitize`）；③ 由轻到重（能折就不摘要、能摘要就不删）。

---

## 1. 总编排 `ChatHistoryPreparer.prepareMessagesForInference`（完整顺序）

每次调 LLM 前执行，返回新列表，不动原文：
① collapseHistoricalUserInlineImages   # 历史 user 内联图片 → 路径文本（省 base64）
② sanitize                              # 配对清洗
③ if (history-compress.enabled):
     sanitize
   → ChatHistoryCompressPipeline.applyIntraAndInterRound   # 【级1 轮内折叠】→【级2 轮外剥离】
   → sanitize
④ capIndividualMessageSizes(maxMessageChars=20000)          # 【级3 单条截断】
⑤ ChatHistoryFinalCompressor.apply(finalMax, minKeepTools)  # 【级4 最终 stage3 删条】
⑥ return sanitize(...)                  # 出口再清一次，保证发给网关的一定合法
- finalMax = LlmModelCompat.inferenceCharBudget(llmProfile, harness)（见第 11 节）。
- minKeepTools = graph.history-trim-min-tool-steps-in-current-round（默认 6）。
- 离线/单测入口 prepareForLlm：先 mapHistoryFromDb（DB 行→OpenAI messages，见第 3 节）再进本管线。
管线本体 ChatHistoryCompressPipeline 极薄：
public static List<Map<String,Object>> applyIntraAndInterRound(List<Map<String,Object>> messages, HarnessHistoryCompressSettings hc) {
    if (messages == null || messages.isEmpty() || hc == null || !hc.isEnabled()) return messages;
    List<Map<String,Object>> s = new ArrayList<>(messages);
    s = ChatHistoryIntraRoundCompressor.apply(s, hc);   // 级1
    s = ChatHistoryInterRoundCompressor.apply(s, hc);   // 级2
    return s;
}

---

## 2. 【重难点】`sanitize` — OpenAI 配对校验（每步都调）

为什么重要：折叠/删条一旦留下"孤儿 tool"(无对应 assistant.tool_calls) 或"不完整 tool_calls"(assistant 声明了却没回执)，下次请求 LLM 直接 400。所以每个压缩动作前后都夹一次 `sanitize`。

规则（OpenAI 链式约束）：每条 `role=tool` 必须回执前面最近一条带 `tool_calls` 的 assistant 里"尚未被回执"的某个 `tool_call_id`。违规两类：
- 孤儿 tool（无匹配 pending id）→ 直接丢弃；
- 不完整 assistant.tool_calls（有 tool_calls 但缺回执）→ 剥掉 `tool_calls`，content 空则填占位说明。

算法骨架：
sanitize(messages):
  out=[]; pendingIds=null; pendingAsstIdx=-1
  for m in messages:
    if role=="tool":
        if pendingIds!=null && pendingIds.remove(m.tool_call_id): out.add(m)   // 命中未回执 id → 合法
        else droppedTool++                                                      // 孤儿 → 丢
        continue
    if pendingIds 非空:                          // 遇到非 tool，上一个 assistant 还有敞口 id → 收尾
        finalizeIncompleteAssistantToolSequence(out, pendingAsstIdx)  // 剥 tool_calls / 填占位
        dropToolMessagesAfterAssistant(out, pendingAsstIdx+1)         // 清半配对 tool
        pendingIds=null
    if role=="assistant":
        pendingIds = extractToolCallIds(m)        // 收集本条 tool_calls[].id；无则 null
        out.add(m); pendingAsstIdx = pendingIds==null ? -1 : out.size()-1
    else: pendingIds=null; out.add(m)
  // 列表结束仍有敞口 → 同样收尾
  return out
> `LlmModelCompat.adaptOutboundMessages` 在真正发请求前再调一次 sanitize，并叠加 DeepSeek `reasoning_content` 兜底——配对校验的最后一道。

---

## 3. `mapHistoryFromDb` — DB 行 → OpenAI messages

DB 存的是结构化 chat_message（role/content/meta），要还原成 OpenAI 格式：
for m in history:
  msg.role = m.role
  body = stripLeadingTaskTagLine(m.content)                 // 剥首行 [task_id:...]（旧版遗留）
  if role==assistant: body = stripLeakedToolCallsFromAssistantContent(body)  // 括号配平剥正文里泄漏的 tool_calls=[...]
  if meta.tool_calls/tool_call_id/name/reasoning_content 存在 → 搬到顶层消息键   // ★ 结构化字段权威来源
  if role==user && meta.attachments 非空:
      body = persistedPlainText(body, att)                  // 每附件一行 "[附件: path]"（不含 base64）
      if 仅最后一条 user 且 agentId 有效: body = ChatUserMultimodalContent.buildForLlm(...)  // 内联 vision
  msg.content = body
return sanitize(raw)
关键：**`tool_calls`/`tool_call_id`/`name` 从 `meta` 还原**（不进正文）；正文里若泄漏了 `tool_calls=[...]` 用括号配平剥掉——`meta` 是权威来源。这保证 DB 原件能重建出完整、可被 retrieve 配对的消息。

---

## 4. 多模态图片折叠 `collapseHistoricalUserInlineImages`

内联图片是 base64（一张几十万字符），极占预算：
lastUser = 最后一条 user 下标
currentRoundHasFollowUp = (lastUser 后已有 assistant/tool)
for m in messages (role==user):
  keepInline = (i==lastUser && !currentRoundHasFollowUp)   // 只有"最后一条 user 且本轮首次推理"保留内联
  if keepInline || 无内联图: 原样
  else: content = collapseMultipartToTextReference(content) // 只留 text 段、删 image_url
为什么：更早轮的图早看过、结论已进后续对话 → 折成路径引用；最后一条 user 只在"首次推理"保留整图，一旦进 ReAct 循环（后面已有 tool）就折掉，避免每步重复携带同一张 base64。（上限：单图内联 4MiB、单附件上传 100MiB；非图附件不内联，只给"工作区路径 + 用 read_file"指引。）

---

## 5. 级1 — 轮内折叠 `ChatHistoryIntraRoundCompressor`（只看步数）

只处理最后一条 user 之后的当前轮。"一步"= 一条带 `tool_calls` 的 assistant + 其后连续 `tool` 回执。按"距末步数 `stepFromEnd`"分档：

距末步数
档
动作
≤ keepRecentSteps(6) / ≤ foldToolContentAfterStep(6)
NONE
不动
≤ summarizeAfterStep(12)
FOLD_TOOL_CONTENT
只折叠 tool content
更早
SUMMARIZE
参数级摘要（见第 7 节）

private static Tier tierForStep(int stepFromEnd, int keepRecent, int foldContentAfter, int summarizeAfter) {
    if (stepFromEnd <= keepRecent)       return Tier.NONE;
    if (stepFromEnd <= foldContentAfter) return Tier.NONE;
    if (stepFromEnd <= summarizeAfter)   return Tier.FOLD_TOOL_CONTENT;
    return Tier.SUMMARIZE;
}
门槛 Math.max 单调钳制：keep ≤ foldAfter ≤ summarizeAfter。

---

## 6. 【重难点·完整源码】折叠核心 `ChatHistoryTieredCompressor`

把长字符串折成「头 + 标记 + 尾」，且穿透 JSON 只折字符串叶子。
private static final String FOLD_MARKER = "……[agent:folded]……";
private static final int TIER1_LEAF_MIN_CHARS = 300;   // ★ 硬编码：叶子 >300 才折
private static final int TIER1_KEEP_HEAD_CHARS = 200;  // ★ 保头 200
private static final int TIER1_KEEP_TAIL_CHARS = 100;  // ★ 保尾 100

private static String foldTier1FieldContent(String raw, HarnessHistoryCompressSettings cfg) {
    String s = raw == null ? "" : raw.trim();
    if (s.isEmpty()) return raw;
    try { int[] c={0}; return MAPPER.writeValueAsString(foldTier1JsonSubtree(MAPPER.readTree(s), cfg, c)); }  // ① 试 JSON
    catch (Exception e) { return foldTier1PlainLeaf(s); }                                                     // ② 非 JSON 整段头尾折
}
// ★ 递归：只在字符串叶子 >300 折；节点数超上限跳过整棵子树
private static JsonNode foldTier1JsonSubtree(JsonNode node, HarnessHistoryCompressSettings cfg, int[] nodeCounter) {
    if (node == null || node.isNull()) return node;
    if (nodeCounter[0]++ > cfg.getMaxJsonNodes()) return TextNode.valueOf("[memory-agent: JSON too large, folding skipped for subtree]");
    if (node.isTextual())              return TextNode.valueOf(foldTier1PlainLeaf(node.asText()));  // 只折字符串叶子
    if (node.isNumber()||node.isBoolean()) return node;  // 数值/布尔原样 → 保结构
    if (node.isArray())  { ArrayNode a=MAPPER.createArrayNode(); for(JsonNode c:node) a.add(foldTier1JsonSubtree(c,cfg,nodeCounter)); return a; }
    if (node.isObject()) { ObjectNode o=MAPPER.createObjectNode(); node.fields().forEachRemaining(e->o.set(e.getKey(),foldTier1JsonSubtree(e.getValue(),cfg,nodeCounter))); return o; }
    return node;
}
private static String foldTier1PlainLeaf(String s) {
    if (s == null || s.length() <= TIER1_LEAF_MIN_CHARS) return s == null ? "" : s;
    return foldMiddle(s, TIER1_KEEP_HEAD_CHARS, TIER1_KEEP_TAIL_CHARS);   // 头200 + 标记 + 尾100
}
思想：折叠穿透 JSON 结构——键名、数值、短字段全留，只把长字符串叶子折成「头+标记+尾」→ 折完仍是合法 JSON、结构可读。和落盘 `clampToMaxChars`（整体到量缩最长叶子）是同一哲学两种手法。

> ⚠️ 代码/配置不一致（如实标注）：`foldTier1PlainLeaf` 用硬编码 300/200/100，`foldTier1JsonSubtree` 只读了 `cfg.getMaxJsonNodes()`。而 `application.yml` 的 `history-compress.json-field-min-chars(200)/json-keep-head(40)/json-keep-tail(40)/plain-*` 等，当前这条折叠路径并未读取——疑似历史/另一折叠实现的遗留配置。复现以代码硬编码为准。

---

## 7. 【重难点·补全】级1 SUMMARIZE — 摘要后到底变成什么样

`ChatHistoryIntraRoundStepSummarizer` 调 `ToolStepNarrationParser`（解析原文槽位）+ `ToolStepSummaryEnricher`（确定性拼装，无 LLM）。产物保留取件号、压缩体量：
- assistant 步 `summarizeAssistantStep`：content 换成结构化摘要头；`stripToolCallArguments` **保留每个 tool_call 的 `id`+`function.name`，只把 `arguments` 置为 `"{}"`**。
- tool 回执 `summarizeToolMessage`：content 换成自带取件号的折叠指针；`preserveCriticalHint` 抽出 `ERROR:` 首行或 `/data/`、`sessions/ses_` 路径塞进去。

产物真实形态：
# assistant.content：
[tool-step-summary step=3] tool_call_ids=call_abc,call_def | tools=read_file,grep
已具备: 已定位主类入口
下一步: 读取 pom.xml 确认依赖版本
narration: <可选>
# 且 tool_calls 结构还在，只是 arguments 变 "{}"

# tool.content（三种 hint）：
[tool-result-folded] tool=read_file | retrieve via retrieve_tool_call("call_abc").
[tool-result-folded] tool=run_build | retrieve via retrieve_tool_call("call_def") | ERROR: cannot find symbol ...
[tool-result-folded] tool=write_file | retrieve via retrieve_tool_call("call_ghi") | path=/data/.../output.log.
一步压缩前后：
// 前
{"role":"assistant","content":"已具备：已定位主类入口\n下一步：读 pom.xml",
 "tool_calls":[{"id":"call_abc","function":{"name":"read_file","arguments":"{\"path\":\"pom.xml\"}"}}]}
{"role":"tool","tool_call_id":"call_abc","name":"read_file","content":"<pom.xml 数千字全文…>"}
// 后
{"role":"assistant","content":"[tool-step-summary step=2] tool_call_ids=call_abc | tools=read_file\n已具备: 已定位主类入口\n下一步: 读 pom.xml",
 "tool_calls":[{"id":"call_abc","function":{"name":"read_file","arguments":"{}"}}]}       // ← id/name 活，arguments 死
{"role":"tool","tool_call_id":"call_abc","name":"read_file","content":"[tool-result-folded] tool=read_file | retrieve via retrieve_tool_call(\"call_abc\")."}
关键：`id`/`name`/`tool_call_id` 三处主键全保留，`retrieve_tool_call("call_abc")` 就是回取坐标。摘要压缩的是体量（arguments、tool 正文），保留的是寻址主键 + 失败信号。

---

## 8. 级2 — 轮外剥离 `ChatHistoryInterRoundCompressor`（字符 + 轮数双闸）

if (est > level2BudgetChars(80k) && userRounds > level2WhenUserRoundsAbove(1))  stripMinRoundsFromEnd = 1;  // level2
else if (est > level1BudgetChars(40k) && userRounds > level1WhenUserRoundsAbove(3)) stripMinRoundsFromEnd = 3;  // level1
// applyTier2Strip：被剥的旧 user 轮只留 user + 末条无 tool_calls 的 assistant（最终交付），中间 tool 步全丢
roundsFromEnd：0=当前轮（豁免），1=上一轮…。level2 剥 ≥1 的轮，level1 剥 ≥3 的轮。

---

## 9. 级3 — 单条截断

capIndividualMessageSizes(maxMessageChars=20000) → 逐条 clampMessageContent：JSON 工具返回走 ToolResultDocument.clampToMaxChars（缩最长 JSON 字符串叶子、保合法），否则朴素 substring + 截断标记；多模态只截 text 块、不动 image_url。

---

## 10. 【重难点·完整源码】级4 — 最终 stage3 删条 `ChatHistoryIntraRoundTrimmer`

整窗仍超 `finalMax` 时删条，当前 user 轮永不删，四步走：
public static List<Map<String,Object>> trim(List<Map<String,Object>> in, int triggerChars, int targetChars, int minKeep) {
    int trigger = max(1024, triggerChars), target = min(max(1024, targetChars), trigger);
    if (est <= trigger) return msgs;                       // 未触发
    int lastUserIdx = indexOfLastUser(msgs);
    int prefixRemoved = trimPrefixBeforeCurrentUser(msgs, target);         // ① 删当前 user 之前整条（清前导孤儿 tool）
    int omitted = 0;
    if (est > target) omitted = trimEarlyToolStepsInCurrentRound(msgs, lastUserIdx, target, minKeep);  // ② 删当前轮早步(保尾部 minKeep)
    if (prefixRemoved>0 || omitted>0) { insertTrimNotice(...); msgs = sanitize(msgs); }                 // ③ 插 trim 便签
    if (est > target) msgs = shrinkLongestUntilUnder(msgs, target, lastUserIdx);                        // ④ 兜底缩最长文本
    return msgs;
}
保配对的关键 `computeKeepFromIndex`：从末尾往回数 `minKeep` 条 tool，再**往前把这些 tool 对应的 `assistant.tool_calls` 一起纳入不可删边界**（否则删了 assistant 留孤儿 tool → 400）。
便签 `insertTrimNotice`：插一条 assistant "上下文已裁剪…完整正文可能仍在磁盘——需要时用 read_file 取。已完成的工具调用别重复做。"——把"取回能力"直接写进提示。

---

## 11. finalMax / `inferenceCharBudget` 推导

configured = max(1024, graph.stage3-trigger-chars)            // 默认 120000；profile 空则直接返回它
charsPerToken = {DEEPSEEK 0.70 | OPENAI_GPT5 0.85 | 其他 0.80}
modelBased = max(32000, (int)(contextLimitTokens × charsPerToken × 0.72))   // 0.72 预留 system+tools+本轮输出
finalMax = min(configured, modelBased)
默认 131072 tokens、stage3-trigger=120000 下：DeepSeek≈66060、GPT5≈80215、其他≈75497。即默认上下文下 `finalMax` 由模型折算值决定；只有上下文极大时才被 120000 反向夹住。`ChatHistoryFinalCompressor.apply` 把 finalMax 同时当 trigger 和 target 传给 trimmer。

---

## 12. 【补全·核心】取回/工具召回 `retrieve_tool_call`

折叠敢激进的底气：折叠只拿走上下文里的副本，原件在 DB，可按 `tool_call_id` 捞回。

### 12.1 工具
- 名 retrieve_tool_call，入参 {tool_call_id}（必填），启用随 file_io。返回：
{ "tool_call_id":"call_…", "source":"run_state|archive|run_state+archive",
  "call":  {"name":"read_file","arguments":"…","assistant_content":"…","arguments_truncated":true?,"arguments_original_chars":N?},
  "result":{"content":"…","truncated":true?,"original_chars":N?,"max_message_chars":M?} }

三段各经 `clampMessageContent(maxChars)` 压到单条上限；`truncated`/`original_chars` 是本次取回为省 token 的二次裁剪（`original_chars` 常能证明 DB 里更长）。找不到 → `ERROR: no tool call exchange found ...`。

### 12.2 两级查找（先内存后 DB）
① 内存 run_state：Optional<Exchange> = ToolCallExchangeLookup.findInMessages(state.messages(), id);   // 可能只被折叠了，还在
② 数据库 archive（分别按需补）：
   if (result 为空)                       result = archive.findByToolCallId(sessionId, id);      // resultSource=archive
   if (call 为空 || arguments 是 "{}"/空) call   = archive.findCallByToolCallId(sessionId, id);  // callSource=archive|mixed
   source = resolveSource(callSource, resultSource)   // 相等→单一；不等→"a+b"（如 archive+run_state）
`ToolCallExchangeLookup.findInMessages` 单趟遍历，**靠 `tool_call_id` 字符串相等配对**（不依赖相邻/顺序），取最后一次匹配；`record CallHit(id,name,arguments,assistantContent) / ResultHit(id,name,content) / Exchange(call,result)`。

### 12.3 "数据库存原件"证据链
findToolMessageByToolCallId:      SELECT * FROM chat_message WHERE session_id=? AND role='tool'      → 遍历比 meta.tool_call_id，取最新
findAssistantMessageByToolCallId: SELECT * FROM chat_message WHERE session_id=? AND role='assistant' → 比 meta.tool_calls[].id，取最新
- `SELECT *` 取整行 content（未折叠、未截断）；`saveMessage` 落库的是原值。
- 折叠只在 `shallowCopyMessage` 副本上改，从不回写 DB → 内存副本 arguments 已是 `{}`、tool 已是指针，从 DB 仍能捞回完整原文。DB 侧复用 `ToolCallExchangeLookup` 同一套配对逻辑。

### 12.4 与落盘衔接（两跳）
若该步工具输出当初就大到落了盘，DB 里存的 tool 回执本身就是探针指针：
retrieve_tool_call(id) → 拿回探针 {path, head, tail, …}  → 再 read_file(path) → 完整正文
折叠指针里 preserveCriticalHint 已把 path=… 塞进去，模型折叠态下就能直接决定去 read_file。

### 12.5 取件号如何存活
消息
折叠后保留
折叠后丢
assistant(tool_calls)
完整 tool_calls 结构(每项 id+name) + content 头行冗余 tool_call_ids=
arguments→"{}"、原正文→摘要
tool 回执
role/tool_call_id/name + 折叠行内嵌 retrieve via retrieve_tool_call("id")
原正文（仅留 ERROR/path 一行 hint）
tool_call_id 就是贯穿「内存折叠副本 ↔ 数据库原件」的唯一稳定主键。



---

## 13. 配置总表（application.yml · graph 段）

key
默认
环境变量
作用
history-compress.enabled
true
MEMORY_AGENT_HISTORY_COMPRESS_ENABLED
级1+级2 总开关
intra-round.keep-recent-steps
6
AGENT_HISTORY_INTRA_KEEP_RECENT_STEPS
轮内保留最近步
intra-round.fold-tool-content-after-step
6
AGENT_HISTORY_INTRA_FOLD_TOOL_AFTER_STEP
折 tool 正文起点
intra-round.summarize-after-step
12
AGENT_HISTORY_INTRA_SUMMARIZE_AFTER_STEP
参数级摘要起点
inter-round.level1-budget-chars
40000
AGENT_HISTORY_INTER_L1_BUDGET_CHARS
轮外 L1 字符闸
inter-round.level1-when-user-rounds-above
3
AGENT_HISTORY_INTER_L1_USER_ROUNDS_ABOVE
轮外 L1 轮数闸
inter-round.level2-budget-chars
80000
AGENT_HISTORY_INTER_L2_BUDGET_CHARS
轮外 L2 字符闸
inter-round.level2-when-user-rounds-above
1
AGENT_HISTORY_INTER_L2_USER_ROUNDS_ABOVE
轮外 L2 轮数闸
max-message-chars
20000
AGENT_MAX_MESSAGE_CHARS
级3 单条截断
stage3-trigger-chars
120000
AGENT_STAGE3_TRIGGER_CHARS
finalMax 全局上界
stage3-target-chars
120000
AGENT_STAGE3_TARGET_CHARS
（FinalCompressor 实际用 finalMax 同时当 trigger/target）
history-trim-min-tool-steps-in-current-round
6
AGENT_HISTORY_TRIM_MIN_TOOL_STEPS
级4 当前轮保留尾部 tool 步
large-tool-result-spill-chars
20000
AGENT_LARGE_TOOL_RESULT_SPILL_CHARS
落盘阈值（独立于折叠）
| ⚠️ `history-compress.json-field-min-chars` 等 | 200/40/40… | — | 当前折叠核心未读取，见第 6 节标注 |

---

## 14. 设计演进（PRJMC0021）

- 2026-05-21 初版：Stage A/B/C/D 四段式。
- 2026-05-31 重构：改为「轮内 / 轮外 / 最终」三层模型；**移除 stage1/2 的"预算循环"与 `step-summary.trigger-chars`**；原"阶段 A½ 参数级摘要"并入轮内第三档。
- 早期基于 `tool-spills/ + manifest.json + seg-NNN` 的分段 spill 已废弃（对结构化 JSON 不灵活、易漏读后半段），改为工作区整份落盘 + 探针指针。
- "折叠回溯"（retrieve_tool_call）提示词在 prompts/role-templates/coding.md。
- 命名注：文档叫"三层"，执行顺序是"四级"（三层压缩 + 一次单条截断），本文按四级讲。
---

## 15. 设计模式与复现不变量

模式
落点
管线/责任链
四级串行，每级幂等、前后 sanitize
组合（递归折叠）
foldTier1JsonSubtree 只折字符串叶子
策略（分档）
tierForStep；inter-round level1/2
保护性裁剪
当前 user 轮永不删；computeKeepFromIndex 保尾部 tool + 其 assistant.tool_calls
指针+按需取回
折叠留 tool_call_id → retrieve_tool_call 两级捞回；落盘留探针 → read_file
确定性摘要
enrich 全由结构本身拼（步号/id/name），narration 来自 parser，无 LLM

复现不变量：
1. 顺序：折图 → sanitize → 轮内折叠 → 轮外剥离 → 单条截断 → 最终删条；每级前后 sanitize。
2. 折叠只碰字符串叶子（硬编码 >300 折头 200+尾 100），保 JSON 合法；节点超 maxJsonNodes 跳过。
3. 摘要：arguments→`{}`、tool→折叠指针，但 id/name/tool_call_id 必留（取件号）。
4. 当前 user 轮永不删；删条保尾部 minKeep(6) 条 tool 及其 assistant.tool_calls 配对；删完插 trim 便签。
5. 压缩只改副本，DB chat_message 永远是完整原件；retrieve_tool_call 两级(内存→archive)按 id 捞回；大回执捞回是探针则再 read_file。
6. finalMax = min(stage3-trigger, ctxTokens×charsPerToken×0.72) 下限 32000。
7. sanitize 保 OpenAI 配对：孤儿 tool 丢、不完整 tool_calls 剥。
---

## 16. 【核心问答】精简副本是代码做的，不是 LLM

> 一句话答：压缩/折叠全程 零 LLM 调用，所有精简动作由 Java 代码确定性完成。

### 16.1 为什么不用 LLM 来压缩？

维度
用 LLM 压缩
用代码压缩（当前方案）
延迟
每步推理前额外 1-2s
毫秒级
确定性
输出不稳定（同内容每次结果不同）
完全确定，id/name/结构不会错
保主键
容易"幻觉"出错误 id
硬代码保证 tool_call_id 原样保留
成本
token 消耗翻倍
零 token
压缩循环
可能递归压缩自己
纯函数变换，无副作用

### 16.2 代码做了什么？

四步全是字符串/结构变换，没有任何语义理解：

① 折叠（foldTier1PlainLeaf）：
   原文超 300 字符 → 取前 200 字符 + "……[agent:folded]……" + 后 100 字符
   数值/布尔/短字段 → 原样保留（不做语义压缩）

② 摘要头（enrichAssistantContent）—— 纯字符串拼接：
   "[tool-step-summary step=3] tool_call_ids=call_abc | tools=read_file\n"
   "已具备: " + slots.hadConditions()   ← 来自正则提取，不是LLM生成
   "下一步: " + slots.nextIntent()

③ 折叠指针（foldedToolContent）—— 纯字符串拼接：
   "[tool-result-folded] tool=read_file | retrieve via retrieve_tool_call(\"call_abc\") | ERROR: ..."

④ 剥参数（stripToolCallArguments）：
   tool_calls[].function.arguments → 字符串替换为 "{}"
   保留 id / name（取件号主键）

`ToolStepNarrationParser`（解析正则提取"已具备/下一步"槽位）和 `ToolStepSummaryEnricher`（拼装摘要头）是整个流水线里最接近"理解语义"的地方——但它们仍然只做正则匹配 + 字符串裁剪，不调用 LLM。

### 16.3 什么时候才有 LLM？

压缩管线本身：永远没有 LLM。

真正调 LLM 是：
- 每轮推理：LLM 读压缩后的上下文、生成回答
- 内存写入（memory-service）：fact-extraction / memory-judge（记忆系统，与历史压缩无关）
- Plan 模式路由/计划生成（与历史压缩无关）
---

## 17. 【核心问答】正则失败的渐进容错机制

ToolStepNarrationParser 提取"已具备/下一步"时，如果 assistant.content 根本没有这两个关键词，正则就匹配不到。设计如何保证不崩？

### 17.1 两级正则 → Slots 始终非 null

// ToolStepNarrationParser.parse(rawContent)
String had = matchFirst(text, LABELED_HAD);    // 正则1: "已具备：..."
String next = matchFirst(text, LABELED_NEXT);  // 正则2: "下一步：..."
if (had.isEmpty() && next.isEmpty()) {
    had = matchFirst(text, NUMBERED_HAD);       // 退化到: "1. ..."
    next = matchFirst(text, NUMBERED_NEXT);     // 退化到: "2. ..."
}
had  = normalizeSlot(had);    // null/空白 → ""
next = normalizeSlot(next);   // null/空白 → ""
String remainder = "";
if (had.isEmpty() && next.isEmpty()) {
    remainder = truncateRemainder(text);  // 两级都没匹配 → 截断原文前300字作为 remainder
} else {
    remainder = extractUnlabeledRemainder(text, had, next);  // 有匹配 → 剩余部分
}
return new Slots(had, next, remainder);   // ★ 永远返回，绝不抛出

四种情况：

情形
had
next
remainder
"已具备：X\n下一步：Y"
X
Y
其余
"1. X\n2. Y"
X
Y
其余
只有自由文本，无关键词
""
""
原文前300字
rawContent 为 null/空
""
""
""

### 17.2 Enricher 容忍空 Slots

// ToolStepSummaryEnricher.enrichAssistantContent
sb.append("已具备: ").append(slotOrNone(slots.hadConditions())).append('\n');
sb.append("下一步: ").append(slotOrNone(slots.nextIntent()));

private static String slotOrNone(String slot) {
    return slot == null || slot.isBlank() ? NONE_SLOT : slot.trim();  // NONE_SLOT = "(none)"
}

正则完全失败时，摘要头是：
[tool-step-summary step=3] tool_call_ids=call_abc | tools=read_file
已具备: (none)
下一步: (none)
narration: <原文前300字截断>   ← remainder 降级填入

关键保证：id/name/tool_call_id（三处取件号）的保留完全不依赖正则——它们从 `tool_calls` 结构直接读取（`ToolStepSummaryEnricher.refsFromToolCalls`），正则只影响"已具备/下一步"两行文字。即使这两行都是"(none)"，`retrieve_tool_call("call_abc")` 仍然完全有效。

### 17.3 折叠指针的 hint 容错

preserveCriticalHint 也是 fail-soft：
// 优先级1: 首行 ERROR:... → 截取前160字
// 优先级2: 第一个 /data/ 或 sessions/ses_ 路径 → 提取路径
// 都没有 → 返回 "" (折叠指针无 hint，仍然合法)

---

## 18. 【核心问答】LLM 输出格式不符的四级处理策略

> 对于项目里确实用 LLM 生成结构化输出的地方（Plan JSON、memory-judge、Handoff 等），如何保证 LLM 不按要求输出时不崩？

注意：历史压缩管线本身无此问题（零 LLM），以下四级策略针对项目中其它用 LLM 做结构化输出的模块。

### 18.1 低风险：优雅降级（narration parser 模式）

适用于：LLM 生成的文本结构不严格、只是"最好有"的场景。

策略：
- 用正则提取，提取不到就用 (none) 占位 / 原文截断填 remainder
- 整个流程继续正常运行，只是摘要头不那么完整
- 代码：ToolStepNarrationParser.parse — 两级正则 → 两级退化 → 永不抛出
### 18.2 中风险：校验 + 有界重试（Plan JSON 模式）

适用于：Plan JSON 是整个 PEC 流程的输入，格式错就没法执行。

策略：
- 解析失败 → 在下一步推理的 system prompt 里注入"上次格式错误，请重试" + 约束
- 有明确的重试上限（max-plan-retries，默认 2）
- 超限 → fail-soft 走 fallback（转为单步 ReAct，而非直接 500）
### 18.3 高风险：平台覆盖（objective_id 模式）

适用于：某个 id 必须精确匹配（如 objective_id），LLM 一旦改动就会破坏任务追踪。

策略：
- 不信任 LLM 输出的 id，由平台侧强制写入（图节点执行前注入，不经过 LLM）
- 即使 LLM 在 content 里写了错误的 id，执行层以平台注入值为准
### 18.4 混合型：容忍解析 + 白名单过滤（judge 结果模式）

适用于：memory-judge 批量返回 JSON，每条决策中 id 必须是已知的。

策略：
解析 LLM 输出 JSON → 逐条校验 id 是否在白名单（recalled memory ids）内
非法 id → 静默丢弃（不执行 DELETE/MERGE）
解析失败 → tryResolveFully 兜底（score ≥ 0.99 且文案重合 → DROP）

代码：MemoryLlmJudgeProcessor 的 ops按recalled id白名单校验，非法静默过滤。

---

## 19. 【场景演示】5 轮对话的完整压缩演进

用一个具体例子走一遍：哪些内容被折叠、哪些被保留、哪些可以被 retrieve 捞回。

### 背景

用户和 Agent 进行了 5 轮对话，每轮 Agent 执行了多个工具步骤：

轮1: 用户"帮我搭建项目结构" → Agent 执行 mkdir/write_file 等 8 步
轮2: 用户"现在配置 pom.xml" → Agent 执行 read_file/edit_file 等 6 步
轮3: 用户"跑一下单测" → Agent 执行 run_test/read_file 等 5 步（有失败）
轮4: 用户"修复错误" → Agent 执行 edit_file/run_test 等 4 步
轮5: 用户"现在帮我写文档" → 正在进行（当前轮）

假设当前到了轮5 Agent 的第 3 步推理，总字符数已超 80k。

### 各轮各步的处理

#### 轮外剥离（级2，对轮1~3）

est > 80k && userRounds > 1 → level2 触发，剥 ≥1 的旧轮：

轮1被剥：只留 user 消息 + 最后一条无 tool_calls 的 assistant（Agent 的最终汇报），中间 8 步的 tool 步全丢。

轮2被剥：同上，只留 user + 最终 assistant。

轮3、轮4：`userRounds > 1` 但这两轮距末端是 2、1（≥1），也被 level2 剥（取决于 stripMinRoundsFromEnd=1 的含义：剥 roundsFromEnd ≥1 的）。

注意：剥离后内存里的折叠副本没有这些步骤，但 DB chat_message 完整保留所有步骤的原件。

#### 轮内折叠（级1，对当前轮5）

当前轮 Agent 已经执行了 3 步：

步号（距末步数）
stepFromEnd
档
动作
步1（最早）
3
FOLD_TOOL_CONTENT（≤12 且 >6）
tool content 折叠
步2
2
NONE（≤6）
不动
步3（最新）
1
NONE
不动

实际默认值 keepRecent=6，所以 3 步全在 NONE 区——此轮不用折。当步数超 6 时才开始折。

假设轮5已到第 10 步（stepFromEnd=10 > 6 但 ≤ 12 → FOLD_TOOL_CONTENT）：

步1（stepFromEnd=10）的 tool 消息——折叠前：
{"role":"tool","tool_call_id":"call_p7q","name":"read_file",
 "content":"<pom.xml 全文 3000 字...>"}
折叠后：
{"role":"tool","tool_call_id":"call_p7q","name":"read_file",
 "content":"[tool-result-folded] tool=read_file | retrieve via retrieve_tool_call(\"call_p7q\")."}

步1 的 assistant 消息——tool_calls.arguments 折叠：
// 折叠前
{"id":"call_p7q","function":{"name":"read_file","arguments":"{\"path\":\"pom.xml\"}"}}
// 折叠后
{"id":"call_p7q","function":{"name":"read_file","arguments":"{}"}}  // ← id/name 保留，arguments 清空

假设步5（stepFromEnd=5 ≤ 6 → NONE，不动），步10（stepFromEnd=10 > 12 → SUMMARIZE）：

步10 进入 SUMMARIZE——assistant 内容替换：
// 原 content（可能几百字）
"已具备：已确认依赖树无冲突\n下一步：修改 spring-boot 版本至 3.2.0\n[一大段分析文字...]"

// 摘要后 content
"[tool-step-summary step=10] tool_call_ids=call_r8s | tools=edit_file
已具备: 已确认依赖树无冲突
下一步: 修改 spring-boot 版本至 3.2.0"

### "取件号"的全程存活

阶段
call_p7q 在哪
原始 DB
chat_message 完整原件，content 是 pom.xml 全文
轮内 FOLD 后
内存副本 tool.content = 折叠指针（含 retrieve_tool_call("call_p7q")），id 仍在
轮外剥离后
若本轮被剥：内存副本无此消息；DB 仍完整
级4 删条后
若删掉了 → trim 便签 + id 不再出现在上下文，但 DB 仍有原件
Agent 需要时
调用 retrieve_tool_call("call_p7q") → 内存级找不到 → DB archive → 返回完整 pom.xml

### 场景：轮3 的 ERROR 步骤如何传递故障信号

轮3 某步执行 run_test 失败，tool 回执首行是 ERROR: 3 tests failed...。

压缩后（SUMMARIZE 档）：
[tool-result-folded] tool=run_test | retrieve via retrieve_tool_call("call_e4f") | ERROR: 3 tests failed...

preserveCriticalHint 把 ERROR 首行（截至 160 字）塞进折叠指针。

即使轮3 被级2 剥离，ERROR 行已在轮4 的 assistant "修复错误" prompt 里作为上下文使用；
轮5 的 Agent 如需追溯，可 retrieve_tool_call("call_e4f") 从 DB 拿回完整错误栈。

---

## 20. 全流程闭环总结图

用户对话历史（DB chat_message，完整原件，永不压缩）
         │
         │ mapHistoryFromDb（还原 tool_calls/tool_call_id 从 meta）
         ▼
完整 OpenAI messages 列表（内存中）
         │
         │ prepareMessagesForInference（每次推理前执行，返回新副本）
         │
    ┌────┴────────────────────────────────────┐
    │  ① 折图片（base64→路径）                 │
    │  ② sanitize（配对清洗）                  │
    │  ③ 轮内折叠（步数分档：折/摘要/不动）     │
    │     └─ FOLD: tool content 头尾折         │
    │     └─ SUMMARIZE: 摘要头 + 取件号保留    │ ← 全部Java代码，零LLM
    │  ④ sanitize                             │
    │  ⑤ 轮外剥离（字符+轮数双闸）             │
    │  ⑥ sanitize                             │
    │  ⑦ 单条截断（≤20000字/条）               │
    │  ⑧ 最终删条（≤finalMax，保当前轮+minKeep）│
    │  ⑨ 出口 sanitize                        │
    └────┬────────────────────────────────────┘
         │
         ▼
压缩后副本（含折叠指针 retrieve_tool_call("id")）
         │
         │ 发给 LLM 推理
         ▼
LLM 读到折叠指针 → 调用 retrieve_tool_call("call_abc")
         │
    ┌────┴────────────────────────────────────┐
    │  ① 内存 run_state 查找（O(n) 遍历）      │
    │  ② 若无/arguments="{}" → DB archive 查  │ ← SELECT * FROM chat_message（未压缩原件）
    └────┬────────────────────────────────────┘
         │
         ▼
返回完整原文 {call:{name,arguments},result:{content}}
（若超限则再次裁剪并标注 truncated+original_chars）
         │
         ▼
若 result.content 是探针 → LLM 再 read_file(path) → 完整正文

三个不变量：
1. DB 是事实（完整原件），内存副本是视图（按需压缩）
2. tool_call_id 是全程唯一主键，任何压缩操作都不得破坏它
3. 压缩是"信息有损但可追溯"——折叠的信息可通过 retrieve/read_file 追回，丢失的只是效率成本
