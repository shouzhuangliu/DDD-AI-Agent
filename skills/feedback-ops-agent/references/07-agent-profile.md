# 阶段 7：Agent 画像

## 目标

从已解决 Case 中提炼当前 Agent 可复用的业务知识，严格按 Agent 隔离。

## 步骤

1. 只处理已确认、已解决的 Case。
2. 提炼能力、故障模式、业务规则、解决模式和偏好。
3. 每条画像关联 Agent ID、来源 Case ID、版本和证据。
4. 未验证的猜测、一次性结论和跨 Agent 数据不得进入长期画像。

## 完成

画像写入后结束当前反馈流程；后续新反馈从 `references/01-intake.md` 重新开始。
