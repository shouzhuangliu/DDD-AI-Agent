# 阶段 1：反馈接收

## 目标

把用户、运维、监控或内部人员的原始描述保存为可追踪反馈，不提前下技术结论。

## 步骤

1. 确认来源、原始描述、服务名称、发生时间、影响对象和联系人。
2. 缺少信息时最多提出三个澄清问题。
3. 调用 `create_feedback` 保存原始内容，保留用户原话。
4. 向用户说明反馈编号和当前状态为“待分类”。

## 禁止

不要承诺修复时间，不要直接标记解决，不要把猜测写成事实。

## 下一步

反馈保存成功后读取 `references/02-classification.md`。
